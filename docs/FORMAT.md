<!-- Generated by TRXInjectionTool --write-format-docs; do not edit by hand. -->

# TRXI injection binary format

This document describes TRXI, format major version 1 —
the injection file format produced by TRXInjectionTool and consumed by TRX.
It is generated from the format DTOs in `sdk/Format/`, which are the
single source of truth for both the serializer and this document.

Design goals, in order:

1. **Game-independence.** Every record has exactly one layout regardless of
   the targeted game; the engine converts canonical records to its native
   per-game structures at load time.
2. **Skippability.** Applicability tests, chunks and blocks are framed with
   type, version and byte size, so readers can skip anything they do not
   understand.
3. **Name-first references.** Content with a name is referenced through the
   symbol table rather than a fixed slot.

All integers are little-endian. There is no implicit padding or alignment;
structures are packed exactly as written.

## Container

```
u32  magic       'TRXI'
u32  formatMajor 1
u32  fileType    see below
u32  rawLength   payload size once inflated
u32  zlibLength  deflated payload size
u8[] payload     zlib (deflate) stream
```

The loader rejects the file with a logged warning when the
magic differs, `formatMajor` is greater than its own, or the payload fails
to inflate to exactly `rawLength` bytes.

The whole payload is a single deflate stream: the shipped corpus is small
(hundreds of files, median a few hundred bytes), one inflate per file is
microseconds, readers cache the inflated payload, and skipping an entire
irrelevant file already happens before decompression, keyed on `fileType`.
Per-chunk compression would compress worse and buy nothing.

### File types

| Value | Name | Gated by |
|---|---|---|
| 0 | GENERAL | always on |
| 1 | BRAID | braid option |
| 2 | TEXTURE_FIX | texture fixes option |
| 3 | PS1_SFX | PS1 SFX option |
| 4 | FLOOR_DATA | floor-data fixes option |
| 5 | LARA_ANIMS | responsive Lara option |
| 6 | ITEM_POSITION | item-position fixes option |
| 7 | PS1_ENEMY | PS1 enemy option |
| 8 | ALTER_ANIM_SPRITE | sprite fixes option |
| 9 | SKYBOX | skybox option |
| 10 | PS1_CRYSTAL | PS1 crystal option |

## Payload

```
applicability tests
u32  chunkCount
chunk * chunkCount
```

## Applicability tests

```
u32  testCount
test * testCount

test:
  u32  type
  u32  version   layout version of this test type, starting at 1
  u32  size      byte size of data
  u8[] data
```

A file applies to the loaded level only when **every** test it carries
passes. An unknown test type or version is skipped by `size` and treated
as **failed**:
an unknown condition cannot be assumed to hold.

### Test 0: ITEM_META (version 1)

| Field | Type | Notes |
|---|---|---|
| index | `s32` |  |
| type | `objref` |  |
| x | `s32` |  |
| y | `s32` |  |
| z | `s32` |  |
| room | `s16` |  |
| angle | `s16` |  |

### Test 1: ROOM_COUNT (version 1)

| Field | Type | Notes |
|---|---|---|
| count | `s32` |  |

### Test 2: ROOM_META (version 1)

| Field | Type | Notes |
|---|---|---|
| index | `s32` |  |
| info | `roominfo` |  |
| xSize | `u16` |  |
| zSize | `u16` |  |

### Test 3: TEXTURE_SAMPLE (version 1)

| Field | Type | Notes |
|---|---|---|
| textureIndex | `s32` |  |
| blendingMode | `u16` |  |
| atlas | `u16` |  |
| vertices | `uv[4]` |  |

### Test 4: GAME_VERSION (version 1)

Written unless the file declares itself game-neutral; a file with no game test applies to every game its content fits.

| Field | Type | Notes |
|---|---|---|
| game | `s32` | 1 = TR1 ... 5 = TR5 |

## Chunks and blocks

```
chunk:
  u32  type
  u32  version     layout version of this chunk type, starting at 1
  u32  blockCount
  u32  dataLength  byte size of data
  u8[] data        blockCount consecutive blocks

block:
  u32  type
  u32  elementCount number of records in data
  u32  dataLength   byte size of data
  u8[] data
```

Unknown chunk or block types, and chunk versions newer than the loader
understands, are skipped by their size with a warning. `dataLength` is
required: after a chunk or block, the loader must be exactly that
many bytes past its header. Empty chunks and blocks are omitted; readers
must not require any particular chunk to exist.

### Chunk 8: SYMBOLS (version 1)

| Block | Name | Payload record | Notes |
|---|---|---|---|
| 40 | SYMBOLS | Symbol | The symbols chunk is always written first when present; later records may reference the table. |

#### Symbol

| Field | Type | Notes |
|---|---|---|
| context | `s32` | SymbolContext |
| slot | `s32` | the local slot this name stands for; records meaning the name write this slot with a symbol marker |
| name | `s32 length, u8[length] text` | ASCII, max 255 bytes |
| flags | `s32` | reserved, 0 |

### Chunk 0: TEXTURE_DATA (version 1)

| Block | Name | Payload record | Notes |
|---|---|---|---|
| 0 | PALETTE | PaletteEntry |  |
| 1 | TEXTURE_PAGES | TexturePages |  |

#### PaletteEntry

| Field | Type | Notes |
|---|---|---|
| colour | `colour` |  |

#### TexturePages

All 32-bit pages' pixels, then all 8-bit pages' pixels; elementCount is the page count.

| Field | Type | Notes |
|---|---|---|
| pixels | `u8[] — elementCount * 256 * 256 pixels of u32, then as many of u8` |  |

### Chunk 1: TEXTURE_INFO (version 1)

| Block | Name | Payload record | Notes |
|---|---|---|---|
| 2 | OBJECT_TEXTURES | ObjectTexture |  |
| 3 | SPRITE_TEXTURES | SpriteTexture |  |
| 4 | SPRITE_SEQUENCES | SpriteSequence |  |

#### ObjectTexture

TR4 superset; the trailing metadata is always computed from source UVs and ignored by converters for games without it.

| Field | Type | Notes |
|---|---|---|
| attribute | `u16` |  |
| tileAndFlag | `u16` |  |
| newFlags | `u16` | 0 when the source had none |
| vertices | `uv[4]` | unused 4th vertex zeroed |
| originalU | `u32` |  |
| originalV | `u32` |  |
| widthMinusOne | `u32` |  |
| heightMinusOne | `u32` |  |

#### SpriteTexture

| Field | Type | Notes |
|---|---|---|
| atlas | `u16` |  |
| x | `u8` |  |
| y | `u8` |  |
| width | `u16` |  |
| height | `u16` |  |
| left | `s16` |  |
| top | `s16` |  |
| right | `s16` |  |
| bottom | `s16` |  |

#### SpriteSequence

For slot-referenced sequences length is negative (the classic convention) and startIndex is 0; for symbol-named sequences length is the positive sprite count and startIndex locates the first sprite texture this file brings.

| Field | Type | Notes |
|---|---|---|
| spriteID | `objref` |  |
| length | `s16` |  |
| startIndex | `u16` |  |

### Chunk 2: MESH_DATA (version 1)

| Block | Name | Payload record | Notes |
|---|---|---|---|
| 5 | OBJECT_MESHES | Mesh |  |
| 6 | MESH_POINTERS | MeshPointers |  |

#### Mesh

One face encoding for all games; records are packed, no trailing alignment. elementCount counts meshes (TRXJ counted 16-bit words).

| Field | Type | Notes |
|---|---|---|
| centre | `vertex` |  |
| collRadius | `s32` |  |
| vertices | `s16 count, vertex[count]` |  |
| normalCount | `s16` | > 0: that many normals follow; < 0: that many lights follow |
| normals | `vertex[] — NormalCount when positive` |  |
| lights | `s16[] — -NormalCount when negative` |  |
| texturedQuads | `u16 count, Face4[count]` |  |
| texturedTriangles | `u16 count, Face3[count]` |  |
| colouredQuads | `u16 count, Face4[count]` | empty for sources without coloured faces |
| colouredTriangles | `u16 count, Face3[count]` |  |

#### Face4

| Field | Type | Notes |
|---|---|---|
| vertices | `u16[4]` |  |
| texture | `u16` |  |
| effects | `u16` | 0 where the source game has none |

#### Face3

| Field | Type | Notes |
|---|---|---|
| vertices | `u16[3]` |  |
| texture | `u16` |  |
| effects | `u16` |  |

#### MeshPointers

| Field | Type | Notes |
|---|---|---|
| pointers | `u32[] — elementCount entries` |  |

### Chunk 3: ANIMATION_DATA (version 1)

| Block | Name | Payload record | Notes |
|---|---|---|---|
| 7 | ANIM_CHANGES | AnimChange |  |
| 8 | ANIM_RANGES | AnimRange |  |
| 9 | ANIM_COMMANDS | AnimCommand |  |
| 10 | ANIM_BONES | AnimBone |  |
| 11 | ANIM_FRAMES | AnimFrame |  |
| 12 | ANIMS | Animation |  |

#### AnimChange

| Field | Type | Notes |
|---|---|---|
| stateID | `u16` |  |
| numAnimDispatches | `u16` |  |
| animDispatch | `u16` |  |

#### AnimRange

| Field | Type | Notes |
|---|---|---|
| low | `s16` |  |
| high | `s16` |  |
| nextAnimation | `s16` |  |
| nextFrame | `s16` |  |

#### AnimCommand

| Field | Type | Notes |
|---|---|---|
| value | `s16` | raw command word |

#### AnimBone

| Field | Type | Notes |
|---|---|---|
| flags | `u32` |  |
| offsetX | `s32` |  |
| offsetY | `s32` |  |
| offsetZ | `s32` |  |

#### AnimFrame

One canonical layout for every game; the engine packs frames into its native form at load.

| Field | Type | Notes |
|---|---|---|
| minX | `s16` |  |
| maxX | `s16` |  |
| minY | `s16` |  |
| maxY | `s16` |  |
| minZ | `s16` |  |
| maxZ | `s16` |  |
| offsetX | `s16` |  |
| offsetY | `s16` |  |
| offsetZ | `s16` |  |
| rotations | `u16 count, FrameRotation[count]` | one rotation per mesh |

#### FrameRotation

| Field | Type | Notes |
|---|---|---|
| x | `s16` | 16-bit angle units |
| y | `s16` |  |
| z | `s16` |  |

#### Animation

TR4 superset; converters drop fields the target game lacks.

| Field | Type | Notes |
|---|---|---|
| frameOffset | `u32` | ordinal of the animation's first frame in this file's ANIM_FRAMES |
| frameRate | `u8` |  |
| frameSize | `u8` |  |
| stateID | `u16` |  |
| speed | `fixed32` |  |
| accel | `fixed32` |  |
| lateralSpeed | `fixed32` | 0 for games without lateral motion |
| lateralAccel | `fixed32` | 0 likewise |
| frameStart | `u16` |  |
| frameEnd | `u16` |  |
| nextAnimation | `u16` |  |
| nextFrame | `u16` |  |
| numStateChanges | `u16` |  |
| stateChangeOffset | `u16` |  |
| numAnimCommands | `u16` |  |
| animCommand | `u16` |  |

### Chunk 4: OBJECT_DATA (version 1)

| Block | Name | Payload record | Notes |
|---|---|---|---|
| 13 | OBJECTS | Model |  |
| 29 | STATIC_OBJECTS | StaticObject |  |

#### Model

| Field | Type | Notes |
|---|---|---|
| iD | `objref` |  |
| numMeshes | `u16` |  |
| startingMesh | `u16` |  |
| meshTree | `u32` |  |
| frameOffset | `u32` | frame ordinal in this file's ANIM_FRAMES; 0xFFFFFFFF = mesh-only model |
| animation | `u16` |  |

#### StaticObject

| Field | Type | Notes |
|---|---|---|
| iD | `u32` |  |
| mesh | `u16` | mesh pointer index |
| visBox | `s16[6]` | minX, maxX, minY, maxY, minZ, maxZ |
| collBox | `s16[6]` |  |
| flags | `u16` |  |

### Chunk 5: SFX_DATA (version 1)

| Block | Name | Payload record | Notes |
|---|---|---|---|
| 14 | SAMPLE_INFOS | Sfx |  |
| 41 | NAMED_SAMPLE_INFOS | Sfx | id is a symbol-table index |

#### Sfx

Converters apply per-game volume scaling and drop pitch/range for games without them.

| Field | Type | Notes |
|---|---|---|
| iD | `s16` | SAMPLE_INFOS: sound id; NAMED_SAMPLE_INFOS: symbol index |
| volume | `u16` | canonical 0-0x7FFF scale |
| chance | `u16` |  |
| pitch | `u8` | 0 where the source game has none |
| range | `u8` | 0 likewise |
| characteristics | `u16` |  |
| samples | `u8 count, union SfxSample[count]` |  |

### Chunk 7: CAMERA_DATA (version 1)

| Block | Name | Payload record | Notes |
|---|---|---|---|
| 30 | CINEMATIC_FRAMES | CinematicFrame |  |
| 38 | FLYBY_CAMERAS | FlybyCamera |  |

#### CinematicFrame

| Field | Type | Notes |
|---|---|---|
| targetX | `s16` |  |
| targetY | `s16` |  |
| targetZ | `s16` |  |
| posZ | `s16` |  |
| posY | `s16` |  |
| posX | `s16` |  |
| fOV | `s16` |  |
| roll | `s16` |  |

#### FlybyCamera

| Field | Type | Notes |
|---|---|---|
| x | `s32` |  |
| y | `s32` |  |
| z | `s32` |  |
| dX | `s32` |  |
| dY | `s32` |  |
| dZ | `s32` |  |
| sequence | `u8` |  |
| index | `u8` |  |
| fOV | `u16` |  |
| roll | `s16` |  |
| timer | `u16` |  |
| speed | `u16` |  |
| flags | `u16` |  |
| roomID | `u32` |  |

### Chunk 6: DATA_EDITS (version 1)

| Block | Name | Payload record | Notes |
|---|---|---|---|
| 17 | FLOOR_EDITS | FloorDataEdit |  |
| 18 | ITEM_POS_EDITS | ItemPosEdit |  |
| 19 | MESH_EDITS | MeshEdit |  |
| 20 | TEXTURE_EDITS | TextureOverwrite |  |
| 21 | ROOM_EDIT_META | RoomEditMeta |  |
| 22 | ROOM_EDITS | union RoomEdit |  |
| 22 | ROOM_EDITS | RoomReface |  |
| 22 | ROOM_EDITS | RoomMoveFace |  |
| 22 | ROOM_EDITS | RoomMoveVertex |  |
| 22 | ROOM_EDITS | RoomRotateFace |  |
| 22 | ROOM_EDITS | RoomAddFace |  |
| 22 | ROOM_EDITS | RoomAddVertex |  |
| 22 | ROOM_EDITS | RoomAddSprite |  |
| 22 | ROOM_EDITS | RoomAddStatic3D |  |
| 22 | ROOM_EDITS | RoomEditStatic3D |  |
| 22 | ROOM_EDITS | RoomSetVertexFlags |  |
| 22 | ROOM_EDITS | RoomSetDoubleSided |  |
| 23 | VIS_PORTAL_EDITS | VisPortalEdit |  |
| 24 | CAMERA_EDITS | CameraEdit |  |
| 25 | FRAME_EDITS | FrameEdit |  |
| 26 | OBJECT_3D_EDITS | Object3DEdit |  |
| 27 | ANIM_CMD_EDITS | AnimCmdEdit |  |
| 28 | SPRITE_EDITS | SpriteEdit |  |
| 31 | OBJ_TYPE_EDITS | ObjTypeEdit |  |
| 32 | FRAME_REPLACE | FrameReplacement |  |
| 33 | ITEM_FLAG_EDITS | ItemFlagEdit |  |
| 34 | ANIM_EDITS | AnimEdit |  |
| 35 | ANIM_TEXTURES | AnimTextureEdit |  |
| 36 | OBJ_LINK_EDITS | ObjLinkEdit |  |
| 37 | ITEM_NAME_EDITS | ItemNameEdit |  |
| 39 | PROPERTY_EDITS | union PropertyEdit |  |
| 39 | PROPERTY_EDITS | ObjectPropertyEdit |  |
| 39 | PROPERTY_EDITS | ItemPropertyEdit |  |
| 42 | ANIM_TEXTURE_ADDS | AnimTextureAdd |  |

#### FloorDataEdit

| Field | Type | Notes |
|---|---|---|
| roomIndex | `s16` |  |
| x | `u16` |  |
| z | `u16` |  |
| fixes | `u32 count, union FdFix[count]` |  |

#### ItemPosEdit

| Field | Type | Notes |
|---|---|---|
| index | `s16` |  |
| angle | `s16` |  |
| x | `s32` |  |
| y | `s32` |  |
| z | `s32` |  |
| room | `s16` |  |

#### MeshEdit

| Field | Type | Notes |
|---|---|---|
| modelID | `objref` |  |
| meshIndex | `s16` |  |
| centre | `vertex` |  |
| collRadius | `s32` |  |
| faceEdits | `u32 count, FaceEdit[count]` |  |
| vertexEdits | `u32 count, VertexEdit[count]` |  |
| faceEffects | `u32 count, FaceEffectEdit[count]` |  |

#### FaceEdit

| Field | Type | Notes |
|---|---|---|
| modelID | `objref` |  |
| meshIndex | `s16` | < 0 = palette entry |
| faceType | `u32` | FACE_TYPE |
| faceIndex | `s16` |  |
| targetFaceIndices | `u32 count, s16[count]` |  |

#### VertexEdit

| Field | Type | Notes |
|---|---|---|
| index | `s16` |  |
| change | `vertex` |  |

#### FaceEffectEdit

| Field | Type | Notes |
|---|---|---|
| faceType | `u32` | FACE_TYPE |
| faceIndex | `s16` |  |
| effects | `u16` |  |
| reflective | `u8` |  |

#### TextureOverwrite

| Field | Type | Notes |
|---|---|---|
| page | `u16` |  |
| x | `u8` |  |
| y | `u8` |  |
| width | `u16` |  |
| height | `u16` |  |
| pixels | `u32[] — Width * Height` |  |

#### RoomEditMeta

Derived allocation counts so the engine can size arrays in its pre-pass.

| Field | Type | Notes |
|---|---|---|
| roomIndex | `s16` |  |
| numVertices | `s16` |  |
| numQuads | `s16` |  |
| numTriangles | `s16` |  |
| numSprites | `s16` |  |
| numStatic3Ds | `s16` |  |
| numSectors | `s16` |  |

#### RoomReface

| Field | Type | Notes |
|---|---|---|
| targetIndex | `s16` |  |
| sourceRoom | `s16` |  |
| sourceFaceType | `u32` | FACE_TYPE |
| sourceIndex | `s16` |  |

#### RoomMoveFace

| Field | Type | Notes |
|---|---|---|
| targetIndex | `s16` |  |
| remaps | `u32 count, VertexRemap[count]` |  |

#### VertexRemap

| Field | Type | Notes |
|---|---|---|
| index | `s16` |  |
| newVertexIndex | `u16` |  |

#### RoomMoveVertex

| Field | Type | Notes |
|---|---|---|
| vertexIndex | `u16` |  |
| dX | `s16` |  |
| dY | `s16` |  |
| dZ | `s16` |  |
| shadeChange | `s16` |  |

#### RoomRotateFace

| Field | Type | Notes |
|---|---|---|
| targetIndex | `s16` |  |
| rotations | `u8` |  |

#### RoomAddFace

| Field | Type | Notes |
|---|---|---|
| sourceRoom | `s16` |  |
| sourceIndex | `s16` |  |
| vertices | `u16[] — 4 for quads, 3 for triangles, by FaceType` |  |

#### RoomAddVertex

| Field | Type | Notes |
|---|---|---|
| x | `s16` |  |
| y | `s16` |  |
| z | `s16` |  |
| lighting | `s16` |  |

#### RoomAddSprite

| Field | Type | Notes |
|---|---|---|
| iD | `s32` |  |
| vertex | `u16` |  |
| frame | `u16` |  |

#### RoomAddStatic3D

| Field | Type | Notes |
|---|---|---|
| x | `s32` |  |
| y | `s32` |  |
| z | `s32` |  |
| angle | `s16` |  |
| intensity | `u16` |  |
| iD | `u16` |  |

#### RoomEditStatic3D

| Field | Type | Notes |
|---|---|---|
| meshIndex | `s32` |  |
| x | `s32` |  |
| y | `s32` |  |
| z | `s32` |  |
| angle | `s16` |  |
| intensity | `u16` |  |

#### RoomSetVertexFlags

| Field | Type | Notes |
|---|---|---|
| vertexIndex | `u16` |  |
| flags | `u16` |  |

#### RoomSetDoubleSided

| Field | Type | Notes |
|---|---|---|
| targetIndex | `s16` |  |
| doubleSided | `u8` |  |

#### VisPortalEdit

| Field | Type | Notes |
|---|---|---|
| baseRoom | `s16` |  |
| linkRoom | `s16` |  |
| portalIndex | `u16` |  |
| vertexChanges | `vertex[4]` |  |

#### CameraEdit

| Field | Type | Notes |
|---|---|---|
| index | `s16` |  |
| x | `s32` |  |
| y | `s32` |  |
| z | `s32` |  |
| room | `s16` |  |
| flag | `u16` |  |

#### FrameEdit

| Field | Type | Notes |
|---|---|---|
| modelID | `objref` |  |
| animIndex | `s32` |  |
| packedYZ | `s16` | ((y & 0x3F) << 10) | (z & 0x3FF) |
| packedXY | `s16` | (x << 4) | ((y & 0xFC0) >> 6) |

#### Object3DEdit

| Field | Type | Notes |
|---|---|---|
| typeID | `s32` |  |
| collidable | `u8` |  |
| visible | `u8` |  |
| collBox | `s16[6]` | minX, maxX, minY, maxY, minZ, maxZ |
| visBox | `s16[6]` |  |

#### AnimCmdEdit

| Field | Type | Notes |
|---|---|---|
| typeID | `objref` |  |
| animIndex | `s32` |  |
| rawCount | `s32` |  |
| totalCount | `s32` |  |

#### SpriteEdit

| Field | Type | Notes |
|---|---|---|
| iD | `objref` |  |
| left | `s16` |  |
| top | `s16` |  |
| right | `s16` |  |
| bottom | `s16` |  |

#### ObjTypeEdit

| Field | Type | Notes |
|---|---|---|
| baseType | `objref` |  |
| targetType | `objref` |  |

#### FrameReplacement

| Field | Type | Notes |
|---|---|---|
| modelID | `objref` |  |
| anims | `s32 count, FrameReplacementAnim[count]` |  |

#### FrameReplacementAnim

| Field | Type | Notes |
|---|---|---|
| animID | `s32` |  |
| frames | `s32 count, u16[count]` |  |

#### ItemFlagEdit

| Field | Type | Notes |
|---|---|---|
| index | `s16` |  |
| type | `objref` |  |
| flags | `u16` |  |

#### AnimEdit

| Field | Type | Notes |
|---|---|---|
| modelID | `objref` |  |
| animIndex | `s32` |  |
| speed | `fixed32` |  |

#### AnimTextureEdit

| Field | Type | Notes |
|---|---|---|
| index | `s32` |  |
| textures | `s32 count, u16[count]` |  |

#### ObjLinkEdit

| Field | Type | Notes |
|---|---|---|
| baseType | `objref` |  |
| sourceType | `objref` |  |

#### ItemNameEdit

| Field | Type | Notes |
|---|---|---|
| index | `s16` |  |
| name | `s32 length, u8[length] text` |  |

#### ObjectPropertyEdit

| Field | Type | Notes |
|---|---|---|
| objectId | `objref` |  |
| properties | `s32 count, Property[count]` |  |

#### Property

| Field | Type | Notes |
|---|---|---|
| name | `s32 length, u8[length] text` |  |
| value | `union PropertyValue` |  |

#### ItemPropertyEdit

| Field | Type | Notes |
|---|---|---|
| itemIndex | `s32` |  |
| properties | `s32 count, Property[count]` |  |

#### AnimTextureAdd

| Field | Type | Notes |
|---|---|---|
| textures | `s32 count, u16[count]` |  |

## Unions

A union writes its discriminator first, then the base's common fields,
then the payload of the matching case.

### FdFix (discriminator: `u32`)

#### Case 0: FdTrigParam

| Field | Type | Notes |
|---|---|---|
| actionType | `u8` |  |
| oldParam | `s16` |  |
| newParam | `s16` |  |

#### Case 1: FdMusicOneShot

*empty payload*

#### Case 2: FdInsert

| Field | Type | Notes |
|---|---|---|
| data | `u32 count, u16[count]` |  |

#### Case 3: FdRoomShift

| Field | Type | Notes |
|---|---|---|
| xShift | `u32` |  |
| zShift | `u32` |  |
| yShift | `s32` |  |

#### Case 4: FdTrigItem

| Field | Type | Notes |
|---|---|---|
| type | `objref` |  |
| room | `s16` |  |
| x | `s32` |  |
| y | `s32` |  |
| z | `s32` |  |
| angle | `s16` |  |
| intensity | `s16` |  |
| flags | `u16` |  |
| name | `s32 length, u8[length] text` |  |

#### Case 5: FdRoomProperties

| Field | Type | Notes |
|---|---|---|
| flags | `u16` |  |
| reverb | `u8` |  |
| flipGroup | `u8` | 255 = unchanged |

#### Case 6: FdTrigType

| Field | Type | Notes |
|---|---|---|
| newType | `u8` |  |

#### Case 7: FdSectorOverwrite

| Field | Type | Notes |
|---|---|---|
| fDIndex | `u16` |  |
| boxIndex | `u16` |  |
| roomBelow | `s16` | -1 = none |
| floor | `s16` |  |
| roomAbove | `s16` |  |
| ceiling | `s16` |  |

#### Case 8: FdGlideCamera

| Field | Type | Notes |
|---|---|---|
| timer | `u8` |  |
| glide | `u8` |  |
| shift | `vertex` |  |

#### Case 9: FdZoneFix

| Field | Type | Notes |
|---|---|---|
| flipOffGround | `u16[] — target game's ground-zone count` |  |
| flipOffFly | `u16` |  |
| flipOnGround | `u16[] — same count` |  |
| flipOnFly | `u16` |  |

#### Case 10: FdPortalOverwrite

| Field | Type | Notes |
|---|---|---|
| wall | `s16` | -1 = unchanged |
| sky | `s16` |  |
| pit | `s16` |  |

#### Case 11: FdClimbInsert

| Field | Type | Notes |
|---|---|---|
| direction | `s32` | bit0 +Z, bit1 +X, bit2 -Z, bit3 -X, bit4 monkey swing |

#### Case 12: FdTrigDelete

*empty payload*

#### Case 13: FdTriangulation

| Field | Type | Notes |
|---|---|---|
| type | `s32` | bit0 floor present, bit1 ceiling present |
| data | `u16[] — floor words then ceiling words, by Type bits` |  |

#### Case 14: FdMineCart

| Field | Type | Notes |
|---|---|---|
| type | `s32` | 0 none, 1 left, 2 right, 3 stop |

#### Case 15: FdMaterial

| Field | Type | Notes |
|---|---|---|
| material | `u8` |  |

#### Case 16: FdSectorExtension

| Field | Type | Notes |
|---|---|---|
| additionalXSectors | `u16` |  |
| additionalZSectors | `u16` |  |

#### Case 17: FdNamedTrigParam

| Field | Type | Notes |
|---|---|---|
| actionType | `u8` |  |
| oldParam | `s16` |  |
| symbol | `symbol` | in place of a raw param |

### PropertyEdit (discriminator: `s32`)

#### Case 0: ObjectPropertyEdit

| Field | Type | Notes |
|---|---|---|
| objectId | `objref` |  |
| properties | `s32 count, Property[count]` |  |

#### Case 1: ItemPropertyEdit

| Field | Type | Notes |
|---|---|---|
| itemIndex | `s32` |  |
| properties | `s32 count, Property[count]` |  |

### PropertyValue (discriminator: `s32`)

#### Case 0: PropInt

| Field | Type | Notes |
|---|---|---|
| value | `s32` |  |

#### Case 1: PropFloat

| Field | Type | Notes |
|---|---|---|
| value | `f32` |  |

#### Case 2: PropDouble

| Field | Type | Notes |
|---|---|---|
| value | `f64` |  |

#### Case 3: PropBool

| Field | Type | Notes |
|---|---|---|
| value | `s32` | 1/0 |

#### Case 4: PropXYZ

| Field | Type | Notes |
|---|---|---|
| x | `s32` |  |
| y | `s32` |  |
| z | `s32` |  |

#### Case 5: PropRGB

| Field | Type | Notes |
|---|---|---|
| value | `colour` |  |

### RoomEdit (discriminator: `u32`)

Common header (after the discriminator):

| Field | Type | Notes |
|---|---|---|
| roomIndex | `s16` |  |
| faceType | `u32` | FACE_TYPE |

#### Case 0: RoomReface

| Field | Type | Notes |
|---|---|---|
| targetIndex | `s16` |  |
| sourceRoom | `s16` |  |
| sourceFaceType | `u32` | FACE_TYPE |
| sourceIndex | `s16` |  |

#### Case 1: RoomMoveFace

| Field | Type | Notes |
|---|---|---|
| targetIndex | `s16` |  |
| remaps | `u32 count, VertexRemap[count]` |  |

#### Case 2: RoomMoveVertex

| Field | Type | Notes |
|---|---|---|
| vertexIndex | `u16` |  |
| dX | `s16` |  |
| dY | `s16` |  |
| dZ | `s16` |  |
| shadeChange | `s16` |  |

#### Case 3: RoomRotateFace

| Field | Type | Notes |
|---|---|---|
| targetIndex | `s16` |  |
| rotations | `u8` |  |

#### Case 4: RoomAddFace

| Field | Type | Notes |
|---|---|---|
| sourceRoom | `s16` |  |
| sourceIndex | `s16` |  |
| vertices | `u16[] — 4 for quads, 3 for triangles, by FaceType` |  |

#### Case 5: RoomAddVertex

| Field | Type | Notes |
|---|---|---|
| x | `s16` |  |
| y | `s16` |  |
| z | `s16` |  |
| lighting | `s16` |  |

#### Case 6: RoomAddSprite

| Field | Type | Notes |
|---|---|---|
| iD | `s32` |  |
| vertex | `u16` |  |
| frame | `u16` |  |

#### Case 7: RoomAddStatic3D

| Field | Type | Notes |
|---|---|---|
| x | `s32` |  |
| y | `s32` |  |
| z | `s32` |  |
| angle | `s16` |  |
| intensity | `u16` |  |
| iD | `u16` |  |

#### Case 8: RoomEditStatic3D

| Field | Type | Notes |
|---|---|---|
| meshIndex | `s32` |  |
| x | `s32` |  |
| y | `s32` |  |
| z | `s32` |  |
| angle | `s16` |  |
| intensity | `u16` |  |

#### Case 9: RoomSetVertexFlags

| Field | Type | Notes |
|---|---|---|
| vertexIndex | `u16` |  |
| flags | `u16` |  |

#### Case 10: RoomSetDoubleSided

| Field | Type | Notes |
|---|---|---|
| targetIndex | `s16` |  |
| doubleSided | `u8` |  |

### SfxSample (discriminator: `u8`)

How a sample's audio data is carried.

#### Case 0: SfxSampleInline

| Field | Type | Notes |
|---|---|---|
| wavData | `u32 count, u8[count]` |  |

#### Case 1: SfxSampleRef

| Field | Type | Notes |
|---|---|---|
| index | `u32` | index into the game's main[_gold].sfx |

## Primitives

#### vertex

| Field | Type | Notes |
|---|---|---|
| x | `s16` |  |
| y | `s16` |  |
| z | `s16` |  |

#### uv

Canonical (TR1/TR2-style) UV encoding, byte order as in classic level files; the TR3 on-disk quirk is normalised by the writer. Each coordinate reads as a u16 little-endian fixed-point value.

| Field | Type | Notes |
|---|---|---|
| uCoord | `u8` |  |
| uPixel | `u8` |  |
| vCoord | `u8` |  |
| vPixel | `u8` |  |

#### colour

Full 8-bit channels; converters shift down for games with VGA palettes.

| Field | Type | Notes |
|---|---|---|
| red | `u8` |  |
| green | `u8` |  |
| blue | `u8` |  |

#### roominfo

Canonical room info; never a game's on-disk room header.

| Field | Type | Notes |
|---|---|---|
| x | `s32` |  |
| z | `s32` |  |
| yBottom | `s32` |  |
| yTop | `s32` |  |

#### fixed32

| Field | Type | Notes |
|---|---|---|
| whole | `s16` |  |
| fraction | `u16` |  |

#### objref

Ids are absolute; games rebase them in their converters. For SYMBOL, id is an index into this file's symbol table.

| Field | Type | Notes |
|---|---|---|
| kind | `s32` | ObjRefKind |
| id | `s32` |  |

#### symbol

Index into this file's symbol table.

| Field | Type | Notes |
|---|---|---|
| index | `s16` |  |

## Versioning policy

- Adding a new chunk, block, or test type: no version change anywhere —
  old readers skip it.
- Changing an existing record's layout: bump that chunk's (or test's)
  version; old readers skip the newer chunk and log a warning.
- Changing the container or the framing rules: bump `formatMajor` — a
  breaking change requiring all files to be regenerated.

The injection tool's plugin handshake is keyed on `formatMajor` only;
chunk-version evolution never invalidates builder plugins.
